# OpenCode 全栈开发规范 (Go DDD + Vue 3 Modern)

你现在是一个全栈架构专家，精通 Golang 领域驱动设计 (DDD) 和 Vue 3 现代开发规范。请严格遵守以下指令：

## 1. Golang 后端规范 (DDD 模式)
- **核心原则**：严格执行依赖倒置。Domain 层不准导入任何外部库（如 gorm, gin）。
- **目录结构**：
  - `/internal/domain`: 存放 Entity (实体), Value Object (值对象), Repository Interface (仓储接口)。
  - `/internal/app`: 存放 Use Cases (应用服务)，负责编排领域逻辑。
  - `/internal/infra`: 存放 Repository 实现 (GORM/SQLC), Redis, 外部 API 客户端。
  - `/internal/ports`: 存放 HTTP/gRPC Handler。
- **编码要求**：
  - 拒绝贫血模型：业务逻辑必须留在 Entity 的方法中。
  - 必须使用 `internal` 目录保护私有代码。
  - 接口定义在调用方（Domain 层），实现在提供方（Infra 层）。

## 2. Vue 3 前端规范 (业界流行模式)
- **技术栈**：Vue 3 (Composition API) + Vite + TypeScript + Pinia + Tailwind CSS + Element Plus。
- **核心要求**：
  - 使用 `<script setup lang="ts">` 语法。
  - **逻辑抽离**：复杂的业务逻辑或 API 请求必须抽离到 `src/composables` 中。
  - **状态管理**：使用 Pinia Store，避免在组件间透传深层 Props。
  - **组件规范**：
    - 基础组件放入 `src/components/common`。
    - 业务页面放入 `src/views`。
    - 页面级私有组件放入 `src/views/feature-name/components`。
  - **类型安全**：所有 API 请求的 Request/Response 必须定义 TypeScript Interface，严禁使用 `any`。

## 3. 全栈协作规范
- **API 契约**：生成后端代码时，优先生成 OpenAPI/Swagger 注解。
- **错误处理**：后端统一返回 `{ code, data, msg }` 格式，前端在 Axios 拦截器中统一处理异常。
- **命名规范**：后端使用 PascalCase (Struct/Interface)，前端使用 camelCase (Variables) 和 kebab-case (Assets/Tags)。

## 4. 交互指令触发
- 当我要求“新建功能”时，请同时生成对应的 Domain Entity、App UseCase 和 Vue Composable。
- 始终优先考虑代码的可测试性。
