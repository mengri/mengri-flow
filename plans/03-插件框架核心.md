# 任务 03: 插件框架核心

## 任务概述
实现插件框架的基础接口、注册表、错误处理和配置Schema规范，为资源插件和触发器插件提供基础能力。

## 上下文依赖
- 任务 01: 基础框架（日志、配置）
- 任务 02: 数据库实体（PluginMeta等）

## 涉及文件
- `internal/infra/plugin/types.go` - 插件接口定义
- `internal/infra/plugin/registry.go` - 插件注册表
- `internal/infra/plugin/errors.go` - 插件错误定义
- `internal/infra/plugin/schema.go` - Schema辅助函数

## 详细步骤

### 3.1 插件接口定义完善
**文件：`internal/infra/plugin/types.go`**
- [ ] 验证并完善现有接口定义：
  - `PluginMeta` - 元数据（名称、类型、版本、描述、作者、BuildTag）
  - `ResourcePlugin` 接口（已实现）
  - `TriggerPlugin` 接口（已实现）
  - `JSONSchema` 类型（已实现）
  - `ToolDefinition` 结构（已实现）
  - `ToolResult` 结构（已实现）
  - `TriggerHandler` 函数签名（已实现）
  - `TriggerResult` 结构（已实现）
- [ ] 添加接口编译时检查：
```go
var _ ResourcePlugin = (*examplePlugin)(nil)
var _ TriggerPlugin = (*exampleTriggerPlugin)(nil)
```

### 3.2 插件注册表实现
**文件：`internal/infra/plugin/registry.go`**
```go
type Registry struct {
    resources map[string]ResourcePlugin
    triggers  map[string]TriggerPlugin
    mu        sync.RWMutex
}

func (r *Registry) RegisterResource(plugin ResourcePlugin)
func (r *Registry) RegisterTrigger(plugin TriggerPlugin)
func (r *Registry) GetResource(name string) (ResourcePlugin, error)
func (r *Registry) GetTrigger(name string) (TriggerPlugin, error)
func (r *Registry) ListResources() []ResourcePlugin
func (r *Registry) ListTriggers() []TriggerPlugin
```
- [ ] 实现注册表单例
- [ ] 线程安全（使用 sync.RWMutex）
- [ ] 插件不存在时返回带类型信息的错误
- [ ] 添加单元测试

### 3.3 全局注册表实例
**文件：`internal/infra/plugin/global.go`**
```go
var globalRegistry *Registry
var once sync.Once

func GlobalRegistry() *Registry {
    once.Do(func() {
        globalRegistry = &Registry{
            resources: make(map[string]ResourcePlugin),
            triggers:  make(map[string]TriggerPlugin),
        }
    })
    return globalRegistry
}
```
- [ ] 实现懒加载单例模式
- [ ] 确保线程安全

### 3.4 插件错误处理规范
**文件：`internal/infra/plugin/errors.go`**
```go
var (
    ErrConnectionFailed     = errors.New("connection failed")
    ErrAuthenticationFailed = errors.New("authentication failed")
    ErrTimeout              = errors.New("execution timeout")
    ErrInvalidInput         = errors.New("invalid input")
    ErrNotFound             = errors.New("resource not found")
    ErrRateLimited          = errors.New("rate limited")
    ErrInternal             = errors.New("internal error")
)

type PluginError struct {
    Type    string // 错误类型标识
    Message string // 错误描述
    Cause   error  // 原始错误
}
```
- [ ] 定义标准错误类型（如上）
- [ ] 实现 `PluginError` 结构体
- [ ] 实现 `Error() string` 方法
- [ ] 实现 `Unwrap() error` 方法支持错误链
- [ ] 添加错误码映射到HTTP状态码的函数

### 3.5 JSON Schema辅助函数
**文件：`internal/infra/plugin/schema.go`**
```go
// BuildStringSchema 构建字符串类型Schema
func BuildStringSchema(title, description string, required bool) JSONSchema

// BuildNumberSchema 构建数字类型Schema
func BuildNumberSchema(title, description string, required bool, min, max *float64) JSONSchema

// BuildObjectSchema 构建对象类型Schema
func BuildObjectSchema(title, description string, properties map[string]JSONSchema, required []string) JSONSchema

// BuildArraySchema 构建数组类型Schema
func BuildArraySchema(title, description string, items JSONSchema) JSONSchema

// BuildEnumSchema 构建枚举类型Schema
func BuildEnumSchema(title, description string, enum []interface{}, defaultValue interface{}) JSONSchema
```
- [ ] 实现Schema构建辅助函数
- [ ] 支持常见字段类型（字符串、数字、布尔、对象、数组、枚举）
- [ ] 支持必填字段标记
- [ ] 支持字段描述和标题
- [ ] 添加单元测试

### 3.6 插件配置管理
**文件：`internal/infra/plugin/config.go`**
```go
// ValidateConfig 根据Schema验证配置
type ConfigValidator struct{}

func (v *ConfigValidator) Validate(schema JSONSchema, config map[string]interface{}) error

// SanitizeConfig 清理配置（移除敏感信息，用于日志）
func SanitizeConfig(config map[string]interface{}, sensitiveKeys []string) map[string]interface{}
```
- [ ] 实现配置验证逻辑
- [ ] 支持根据JSON Schema验证必填字段、类型、格式
- [ ] 实现配置脱敏工具函数
- [ ] 添加单元测试

### 3.7 插件模板生成器（命令行工具）
**文件：`cmd/plugin-gen/main.go`**
- [ ] 创建脚手架命令，快速生成插件模板
- [ ] 支持资源插件模板：`go run cmd/plugin-gen/main.go --type=resource --name=http`
- [ ] 支持触发器插件模板：`go run cmd/plugin-gen/main.go --type=trigger --name=timer`
- [ ] 生成的模板包含：
  - 正确的 build tag
  - 完整的接口实现框架
  - 单元测试模板
  - Schema示例

## 验收标准
- [ ] 所有接口定义清晰，符合插件开发规范
- [ ] 注册表线程安全，支持并发读写
- [ ] 错误类型覆盖所有常见场景
- [ ] Schema辅助函数易用，减少重复代码
- [ ] 插件模板可正常运行并编译
- [ ] 单元测试覆盖率 > 80%

## 参考文档
- `docs/plugin-development-guide.md` - 插件开发规范
- `docs/architecture-design.md` - 插件架构设计
- `plugins/plugin-development-guide.md` - 详细接口定义

## 预估工时
3-4 天
