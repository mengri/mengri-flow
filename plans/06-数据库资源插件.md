# 任务 06: 数据库资源插件

## 任务概述
实现MySQL和PostgreSQL数据库资源插件，支持连接数据库、执行SQL查询和从SQLc文件批量导入工具。

## 上下文依赖
- 任务 03: 插件框架核心
- 任务 02: 数据库实体

## 涉及文件
- `plugins/resource/mysql/plugin.go` - MySQL插件
- `plugins/resource/postgres/plugin.go` - PostgreSQL插件
- `plugins/resource/database/sqlc_parser.go` - SQLc文件解析器（共享）

## 详细步骤

### 6.1 MySQL插件实现
**文件：`plugins/resource/mysql/plugin.go`**
```go
//go:build mysql

package mysql

import (
    "context"
    "database/sql"
    "fmt"
    "time"
    
    _ "github.com/go-sql-driver/mysql"
    "backend/internal/infra/plugin"
)

func init() {
    registry := plugin.GlobalRegistry()
    registry.RegisterResource(&MySQLPlugin{})
}

type MySQLPlugin struct{}
```

#### 6.1.1 插件元数据
```go
func (p *MySQLPlugin) PluginMeta() plugin.PluginMeta {
    return plugin.PluginMeta{
        Name:        "mysql",
        Type:        plugin.PluginTypeResource,
        Version:     "1.0.0",
        Description: "MySQL数据库资源插件",
        Author:      "Platform Team",
        BuildTag:    "mysql",
    }
}
```

#### 6.1.2 配置Schema
```go
func (p *MySQLPlugin) ConfigSchema() plugin.JSONSchema {
    return plugin.JSONSchema{
        "type": "object",
        "required": []string{"host", "port", "database", "username"},
        "properties": map[string]interface{}{
            "host": map[string]interface{}{
                "type":        "string",
                "title":       "主机地址",
                "default":     "localhost",
            },
            "port": map[string]interface{}{
                "type":        "integer",
                "title":       "端口",
                "default":     3306,
                "minimum":     1,
                "maximum":     65535,
            },
            "database": map[string]interface{}{
                "type":        "string",
                "title":       "数据库名",
            },
            "username": map[string]interface{}{
                "type":        "string",
                "title":       "用户名",
            },
            "password": map[string]interface{}{
                "type":        "string",
                "title":       "密码",
                "format":      "password",
            },
            "charset": map[string]interface{}{
                "type":        "string",
                "title":       "字符集",
                "default":     "utf8mb4",
            },
            "maxOpenConns": map[string]interface{}{
                "type":        "integer",
                "title":       "最大连接数",
                "default":     10,
                "minimum":     1,
                "maximum":     100,
            },
            "maxIdleConns": map[string]interface{}{
                "type":        "integer",
                "title":       "最大空闲连接数",
                "default":     5,
                "minimum":     1,
            },
        },
    }
}
```

#### 6.1.3 连接测试
```go
func (p *MySQLPlugin) TestConnection(ctx context.Context, config map[string]interface{}) error {
    dsn := p.buildDSN(config)
    
    db, err := sql.Open("mysql", dsn)
    if err != nil {
        return &plugin.PluginError{
            Type:    "INVALID_CONFIG",
            Message: "invalid connection configuration",
            Cause:   err,
        }
    }
    defer db.Close()
    
    ctx, cancel := context.WithTimeout(ctx, 5*time.Second)
    defer cancel()
    
    if err := db.PingContext(ctx); err != nil {
        return &plugin.PluginError{
            Type:    "CONNECTION_FAILED",
            Message: "failed to connect to MySQL server",
            Cause:   plugin.ErrConnectionFailed,
        }
    }
    
    return nil
}

func (p *MySQLPlugin) buildDSN(config map[string]interface{}) string {
    host := config["host"].(string)
    port := config["port"].(float64)
    database := config["database"].(string)
    username := config["username"].(string)
    password := config["password"].(string)
    
    charset := "utf8mb4"
    if c, ok := config["charset"].(string); ok {
        charset = c
    }
    
    return fmt.Sprintf("%s:%s@tcp(%s:%d)/%s?charset=%s&parseTime=true",
        username, password, host, int(port), database, charset)
}
```

#### 6.1.4 工具执行
```go
func (p *MySQLPlugin) ExecuteTool(
    ctx context.Context,
    resourceConfig map[string]interface{},
    toolConfig map[string]interface{},
    input interface{},
) (*plugin.ToolResult, error) {
    start := time.Now()
    
    // 1. 获取数据库连接（或从连接池）
    db, err := p.getDBConnection(resourceConfig)
    if err != nil {
        return nil, err
    }
    
    // 2. 获取SQL语句
    sql := toolConfig["sql"].(string)
    
    // 3. 执行查询
    rows, err := db.QueryContext(ctx, sql)
    if err != nil {
        return nil, &plugin.PluginError{
            Type:    "EXECUTION_FAILED",
            Message: fmt.Sprintf("query execution failed: %v", err),
            Cause:   err,
        }
    }
    defer rows.Close()
    
    // 4. 解析结果
    result, err := p.parseRows(rows)
    if err != nil {
        return nil, err
    }
    
    duration := time.Since(start)
    
    return &plugin.ToolResult{
        StatusCode: 200,
        Data:       result,
        Duration:   duration,
    }, nil
}
```

#### 6.1.5 结果解析
```go
func (p *MySQLPlugin) parseRows(rows *sql.Rows) ([]map[string]interface{}, error) {
    columns, err := rows.Columns()
    if err != nil {
        return nil, err
    }
    
    var results []map[string]interface{}
    
    for rows.Next() {
        // 创建扫描目标
        values := make([]interface{}, len(columns))
        valuePtrs := make([]interface{}, len(columns))
        for i := range values {
            valuePtrs[i] = &values[i]
        }
        
        if err := rows.Scan(valuePtrs...); err != nil {
            return nil, err
        }
        
        // 转换为map
        row := make(map[string]interface{})
        for i, col := range columns {
            row[col] = values[i]
        }
        
        results = append(results, row)
    }
    
    if err := rows.Err(); err != nil {
        return nil, err
    }
    
    return results, nil
}
```

#### 6.1.6 SQLc文件解析
```go
// ExtractTools 从SQLc文件导入工具
func (p *MySQLPlugin) ExtractTools(ctx context.Context, config map[string]interface{}) ([]plugin.ToolDefinition, error) {
    // 读取sqlc文件
    // 解析命名查询
    // 提取参数和返回字段
    // 生成ToolDefinition列表
    return nil, nil
}
```

### 6.2 PostgreSQL插件实现
**文件：`plugins/resource/postgres/plugin.go`**
```go
//go:build postgres

package postgres

import (
    "context"
    "database/sql"
    _ "github.com/lib/pq"
    // ... 类似MySQL
)
```

PostgreSQL实现与MySQL类似，主要区别：
- [ ] 使用 `github.com/lib/pq` 驱动
- [ ] DSN格式不同：`host=%s port=%d user=%s password=%s dbname=%s sslmode=%s`
- [ ] 支持SSL模式配置（disable/require/verify-ca/verify-full）
- [ ] JSON/JSONB类型处理

#### 6.2.1 PostgreSQL配置Schema
```go
func (p *PostgreSQLPlugin) ConfigSchema() plugin.JSONSchema {
    return plugin.JSONSchema{
        "type": "object",
        "required": []string{"host", "port", "database", "username"},
        "properties": map[string]interface{}{
            // host, port, database, username, password 类似MySQL
            "sslMode": map[string]interface{}{
                "type":        "string",
                "title":       "SSL模式",
                "enum":        []string{"disable", "require", "verify-ca", "verify-full"},
                "default":     "disable",
            },
        },
    }
}
```

### 6.3 连接池管理
```go
type connectionPool struct {
    sync.RWMutex
    connections map[string]*sql.DB
}

var pool = &connectionPool{
    connections: make(map[string]*sql.DB),
}

func (p *MySQLPlugin) getDBConnection(config map[string]interface{}) (*sql.DB, error) {
    // 1. 生成连接标识（DSN的hash）
    // 2. 从连接池获取或创建新连接
    // 3. 设置连接参数（maxOpenConns, maxIdleConns）
}
```
- [ ] 实现连接池，避免重复创建连接
- [ ] 考虑连接复用和清理机制

### 6.4 SQLc解析器（共享）
**文件：`plugins/resource/database/sqlc_parser.go`**
```go
package database

import (
    "bufio"
    "regexp"
    "strings"
    "backend/internal/infra/plugin"
)

// SQLcQuery 表示一个SQLc命名查询
type SQLcQuery struct {
    Name   string
    SQL    string
    Params []SQLParam
}

type SQLParam struct {
    Name string
    Type string
}

// ParseSQLcFile 解析SQLc文件
func ParseSQLcFile(content string) ([]SQLcQuery, error) {
    // 1. 按 -- name: 分割
    // 2. 提取查询名称和SQL
    // 3. 从SQL中提取参数（:param_name 格式）
    
    return nil, nil
}
```
- [ ] 解析SQLc文件格式
- [ ] 提取命名查询
- [ ] 识别SQL参数
- [ ] 转换为ToolDefinition

### 6.5 单元测试
**MySQL测试：**
```go
func TestMySQLPlugin_PluginMeta(t *testing.T)
func TestMySQLPlugin_ConfigSchema(t *testing.T)
func TestMySQLPlugin_TestConnection(t *testing.T)
func TestMySQLPlugin_ExecuteTool_Query(t *testing.T)
func TestMySQLPlugin_ExecuteTool_Insert(t *testing.T)
func TestParseSQLcFile(t *testing.T)
```
- [ ] 使用 `testcontainers-go` 或 sqlmock 进行测试
- [ ] 测试查询和写入操作
- [ ] 测试错误处理

**PostgreSQL测试：**
- [ ] 类似MySQL的测试覆盖

### 6.6 更新插件配置
**文件：`plugins/plugins.yaml`**
```yaml
version: "1.0"
build_tags:
  - http
  - grpc
  - mysql
  - postgres
```
- [ ] 添加 `mysql` 和 `postgres` 到构建配置

## 验收标准
- [ ] MySQL插件完整实现
- [ ] PostgreSQL插件完整实现
- [ ] 支持连接池管理
- [ ] 支持SQLc文件导入
- [ ] 单元测试覆盖率 > 70%
- [ ] 可成功编译：`go build -tags mysql,postgres`
- [ ] 连接测试准确
- [ ] 查询和写入操作正确

## 技术难点
- SQLc文件格式解析（不同项目可能有差异）
- 连接池管理和连接泄漏预防
- 不同数据库类型的差异处理

## 参考文档
- `docs/plugin-development-guide.md` - 资源插件规范
- `docs/PRD.md` - 资源类型配置

## 约束条件

### 性能指标
- 连接测试 **< 5秒** 超时
- SQL 查询执行 **< 30秒**（可配置）
- 表结构导入 **< 10秒**
- 支持 **100+** 并发查询
- 连接池：最小 2，最大 20，最大生命周期 1 小时

### 安全要求（关键！）
- **必须**使用参数化查询（**严禁**字符串拼接）
- **必须**验证 SQL 类型（只允许 `SELECT`）
- **必须**限制查询超时（默认 30 秒）
- **必须**限制返回行数（最大 10000）
- **必须**限制单字段大小（最大 1MB）
- **必须**在日志中脱敏连接串（不输出密码）

### 可靠性要求
- **必须**实现连接池管理（自动重连）
- **必须**处理数据库断开（连接超时、网络错误）
- **必须**验证连接有效性（心跳检测）
- **必须**优雅关闭连接（`defer db.Close()`）

### 测试要求
- **必须**测试连接池参数
- **必须**测试 SQL 注入防护（参数化查询）
- **必须**测试查询超时和限制
- **必须**测试表结构导入（各种字段类型）
- **必须**测试错误处理（连接失败、查询错误）

## 验收标准
- [ ] 插件符合插件开发规范
- [ ] 支持 MySQL 和 PostgreSQL 两种数据库
- [ ] 支持标准 SQL 查询执行
- [ ] 支持从表结构批量导入工具
- [ ] 实现 SQL 注入防护（参数化查询）
- [ ] 单元测试覆盖率 > 80%
- [ ] 使用 `go build -tags database` 可成功编译
- [ ] 连接测试准确反映数据库状态

## 参考文档
- `docs/plugin-development-guide.md` - 资源插件规范
- `docs/PRD.md` - 资源类型配置

## 预估工时
4-5 天

---

## 🎯 AI生成指令

### 你必须遵守的铁律：
1. **不要**生成任何超出本任务范围的代码
2. **不要**修改或重构已有的文件（除非本任务明确要求）
3. **必须**在代码中添加关键注释说明设计决策
4. **必须**在 `init()` 中注册插件到全局注册表
5. **必须**添加接口编译时检查
6. **必须**实现所有接口方法（元数据、Schema、连接测试、工具执行、导入）
7. **必须**返回标准化的 `PluginError` 类型
8. **必须**验证所有配置参数（DSN、SSL 等）
9. **必须**使用参数化查询（**严禁**字符串拼接）
10. **必须**在日志中脱敏敏感信息（密码）

### ⚠️ 常见陷阱提醒（关键！）：
- [ ] **不要忘记**在 `replace_in_file` 时使用**精确的旧代码字符串**
- [ ] **不要忘记**在文件顶部添加 `//go:build database`
- [ ] **不要忘记**在 `plugins/plugins.yaml` 中注册 `database` 标签
- [ ] **必须**使用参数化查询（**严禁** `fmt.Sprintf("SELECT * FROM users WHERE id = %s", id)`）
- [ ] **不要忘记**设置连接池参数（`SetMaxOpenConns`, `SetMaxIdleConns`）
- [ ] **不要忘记**在查询中使用 `WithContext(ctx)`
- [ ] **不要忘记**限制查询结果集（`LIMIT` 或 `sql.MaxRows`）
- [ ] **不要在日志中输出**密码或完整连接串
- [ ] **不要忘记**验证 SQL 类型（只允许 `SELECT`）
- [ ] **不要忘记**测试 SQL 注入防护

### SQL执行示例（关键！）：
```go
// ✅ 正确 - 使用参数化查询
func (p *DatabasePlugin) executeQuery(ctx context.Context, db *sql.DB, query string, args []interface{}) ([]map[string]interface{}, error) {
    // 验证查询类型（只允许 SELECT）
    if !strings.HasPrefix(strings.TrimSpace(strings.ToUpper(query)), "SELECT") {
        return nil, &plugin.PluginError{
            Type:    "INVALID_INPUT",
            Message: "only SELECT queries are allowed",
        }
    }
    
    // 添加限制（防止返回过多数据）
    if !strings.Contains(strings.ToUpper(query), "LIMIT") {
        query += " LIMIT 10000"
    }
    
    // 使用参数化查询
    rows, err := db.QueryContext(ctx, query, args...)
    if err != nil {
        return nil, fmt.Errorf("query execution failed: %w", err)
    }
    defer rows.Close()
    
    // 处理结果...
    return results, nil
}

// ❌ 错误 - SQL 注入风险
query := fmt.Sprintf("SELECT * FROM users WHERE id = '%s' AND name = '%s'", userInputID, userInputName)
// 攻击示例：userInputID = "1'; DROP TABLE users; --"

// ✅ 正确 - 参数化查询
defer rows.Close()
    
    // 处理结果...
    return results, nil
}
```

### 📝 完成标准：
1. 运行测试：`GOTOOLCHAIN=local go test ./plugins/resource/database/... -v -race -cover`
2. 检查lint：`golangci-lint run ./plugins/resource/database/...`
3. 验证构建：`go build -tags database ./plugins/resource/database/`
4. 手动测试连接（使用真实数据库）
5. 测试 SQL 注入防护（尝试注入攻击）
6. 提供简要的完成总结
7. **调用 `open_result_view` 展示主要交付文件**

### 📚 必须参考：
- `plans/00-AI生成提示词优化指南.md` - 所有强制规范
- `AGENTS.md` - 项目整体规范
- `docs/plugin-development-guide.md` - 插件开发详细规范
- 本文档开头的"项目规范提醒"章节

**关键提醒**：数据库插件涉及 SQL 执行，安全性是首要考虑因素。务必确保参数化查询和 SQL 注入防护的正确实现！
